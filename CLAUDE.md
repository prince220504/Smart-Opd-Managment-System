# CLAUDE.md — Smart OPD Management System

Auto-loads when Claude Code opens this folder. Project brief + how to help. **Full day-by-day history lives in `PROJECT_LOG.md` (not auto-loaded).** Feature/permission source of truth: `opd_roles_and_final_structure.html`. Local feature catalogue: `OPD_PROJECT_STATUS.md` (gitignored).

## Who is building this

**Prince** — learning Django from scratch, this project is the teaching vehicle.
- **Types every line himself.** Never pre-scaffold apps/models/views/templates or run `startproject`/`startapp` for him.
- Teach step-by-step: **what** / **why** / **how** for each command + file. Beginner-to-intermediate; explain MVT, migrations, ORM, DRF as they come up.
- Frontend built in Stitch (AI UI) + Antigravity IDE — backend/Django is the learning focus.

## What we're building

Hospital **OPD** system, 4 roles:

| Role | prefix | Core job |
|------|--------|----------|
| **Doctor** | `/doctor/*` | availability, see appointments, prescriptions, request lab tests, view results |
| **Reception/Admin** | `/reception/*` | manage users, book on behalf, all appointments, dashboard |
| **Lab Tech** | `/lab/*` | pending tests, upload PDF/image results, branded reports |
| **Patient** | `/patient/*` | find doctors, book/cancel, view prescriptions & lab results, history |

**Permission rules**: only Reception manages users; only Doctor writes prescriptions + requests lab tests; only Lab Tech uploads results; Patient+Doctor get email, only Patient gets 24-hr reminder.

## Tech stack

- **Backend**: Django 6.0.6 · DRF · SimpleJWT · drf-spectacular · Pillow (images, when needed) · python-decouple · **celery 5.6.3 + redis client (18e)** · psycopg2-binary · Whitenoise · Gunicorn. django-filter/ReportLab: not installed — decide per step (django-filter skipped 14d; ReportLab built then removed at 16d — direct file download beat a generated PDF). `django-celery-beat` + `CELERY_RESULT_BACKEND` deliberately skipped at 18e.
- **Infra (dev)**: Redis in Docker — `docker start opd-redis` each session. 4 terminals: redis · runserver · `celery -A config worker -l info --pool=solo` · `celery -A config beat -l info`.
- **Frontend**: Django templates + static via Whitenoise; HTML drafted in Stitch, polished in Antigravity.
- **DB**: SQLite dev / PostgreSQL prod (Render). Python 3.14.2, venv in `.venv/`.
- **Deploy**: Render (no Docker). `requirements.txt` at repo root; `Procfile` in `backend/` (`web: gunicorn config.wsgi --chdir backend`). Auto-deploy on push to `main`.

## Folder structure

```
opd-project/                  ← repo root
├── backend/
│   ├── config/               ← settings.py, urls.py, wsgi.py
│   ├── apps/                  ← accounts, appointments, lab, prescriptions, notifications, api
│   ├── media/  (gitignored)   .env  (gitignored)  .env.example  manage.py  Procfile
├── frontend/
│   ├── templates/  (base.html + per-app dirs — Django reads here)
│   ├── static/     (css/js/img)   stitch_exports/
├── requirements.txt  (MUST be at root for Render)   .gitignore   README.md
```

Settings point at frontend: `TEMPLATES['DIRS']=[BASE_DIR.parent/'frontend'/'templates']`, `STATICFILES_DIRS=[.../'frontend'/'static']`, `MEDIA_ROOT=BASE_DIR/'media'`. `AUTH_USER_MODEL='accounts.CustomUser'`. `USE_TZ=True` + `TIME_ZONE='Asia/Kolkata'` (UTC in DB, IST on display).

## Conventions

- All folder names **lowercase** (Render = Linux, case-sensitive).
- `requirements.txt` at **root** (Render auto-detect); `Procfile` in `backend/` (`--chdir backend`).
- `.env` never committed; `.env.example` lists keys.
- One feature = one app in `backend/apps/`.
- **One parent step = one branch = one PR** (Option A). Commits: Conventional Commits, atomic. **User runs all git commands himself** — Claude gives commands only.

## Notification matrix (amended Day 38 — email trimmed)

Booking created (both entry points) → Patient (**email**+in-app) · 24-hr reminder → Patient (email+in-app, ✅ 18e Beat 08:00 daily, idempotent via `Appointment.reminder_sent`) · Test requested → Lab Tech (in-app) · Result uploaded → Patient (**email**+in-app) + Doctor (**in-app only**) · Cancelled → other party (in-app) · Confirmed → Patient (in-app) · Prescription written → Patient (in-app) · Walk-in → Reception (in-app) · **Auto-expired stale PENDING → Patient (in-app, 18e Beat 00:30; PENDING only, never CONFIRMED)**.

**Rule (Prince's call, Day 38): email is for people who aren't in the app; in-app for people who live in it.** Doctor sees 30 consults/day with the tab open → mailing every result = alert fatigue → they stop reading email entirely. Original matrix said result→patient+doctor email; doctor email dropped. 3 of 9 events mail (booking · result · reminder).

## Teaching roadmap

Steps 1–12 (foundation → auth): ✅ venv, Django, startproject, runserver, settings, git workflow, `accounts` app, `CustomUser` (role TextChoices), migrations, admin+superuser, URLs/views, templates, auth forms (login/register/profile, PATIENT-locked). Detail in `PROJECT_LOG.md` + `tutorial/`.

- [x] **Step 13** — `appointments` app ✅ (13a–13d, PR #3 `49887e7`). Appointment model (patient CASCADE / doctor PROTECT FKs, date+time_slot fields, status TextChoices), patient booking (ModelForm + IDOR-scoped views), doctor today-view, reception book-on-behalf + global list + filters + confirm. Role-aware nav + login redirect. `_redirect_after_action` helper.
- [x] **Step 13e** — lifecycle + time-split ✅ (PR #4 `1be41ee`). Status += COMPLETED + NO_SHOW (3 terminal states); complete/no-show views; terminal-state-aware buttons; `doctor_history` (`__lte`) + `doctor_upcoming` (`__gt`).
- [x] **Step 14** — DRF layer ✅ (14a–14e, PR #5 `299f45d`). Double-booking `UniqueConstraint` (excl CANCELLED) + form `clean()`; `AppointmentSerializer`; role-scoped `AppointmentViewSet` + router in dedicated `api` app; SimpleJWT (`/api/token/`); drf-spectacular (`/api/docs/`).
- [x] **Step 15** — availability + cancel reason ✅ (15a–15d, PR #6 `57f4f71`). Optional `cancel_reason` shown to all roles. `DoctorAvailability` (recurrence EVERYDAY/WEEKDAYS/MON_SAT/DATE + hours + JSON breaks) = **validation layer, NOT slots** (patient booking UX unchanged; `_validate_doctor_available` in both booking forms). Schedule setup w/ unlimited breaks (getlist+cloneNode) + first-login gate + view/update (`?edit=1`).
- [x] **Step 16 — lab module** ✅ (16a–16e, Day 31, PR #7) — branch `feature/lab-module`. Auth-extras (forgot-pw, profile-photo) that shared the old step title **moved to the frontend step** (forgot-pw needs Step 18 SMTP; both frontend-shaped — that step is now Step 20 after the Day 42 swap).
  - [x] **16a** ✅ (Day 27) — `lab` app + `LabTest` (appointment/requested_by FK PROTECT, status REQUESTED/IN_PROGRESS/DONE, `ordering=['requested_at']`) + `LabResult` (**first OneToOneField** test→result CASCADE; **first FileField** `upload_to='lab_results/'`, no Pillow) + admin + dev media serving (`static(MEDIA_URL,...)`, self-disables when DEBUG=False). Migration `lab.0001`. Commits `acd3f69`, `f552368`.
  - [x] **16b** ✅ (Day 28) — `request_test` (doctor-only, scoped `doctor=me,status=CONFIRMED` lookup = auth, one-field POST no ModelForm) + `lab_queue` (LAB-gated, `status__in=['REQUESTED','IN_PROGRESS']` + `select_related('appointment__patient','requested_by')`, oldest-first). Routes `lab:queue`+`lab:request_test`, request-test buttons on doctor CONFIRMED rows (stub — real page in the frontend step, now Step 20), LAB nav + login redirect. Typo: `appointment:` singular. Commits `ca1227f`, `a1dd151`.
  - [x] **16c** ✅ (Day 29) — `LabResultForm` (ModelForm: result_file/notes/is_normal; test+uploaded_by server-set). `start_test` (LAB-gated, `@require_POST`, from-state in lookup `status=REQUESTED` → IN_PROGRESS) + `upload_result` (GET form/POST save, `LabResultForm(request.POST, request.FILES, instance=existing)` → create-or-update, flips test → DONE). `enctype="multipart/form-data"` + `request.FILES` = the 2 pieces that make files arrive. **OneToOne trap**: 2nd result → `IntegrityError UNIQUE test_id`; fixed with `instance=getattr(test,'result',None)` (RelatedObjectDoesNotExist subclasses AttributeError → getattr default works). Upload page + queue Start/Upload buttons. Typos: `IN_PORGRESS`, `{url` missing `%` (rendered literal into href → 404 with `%7Burl...`). Commits `9af644e`, `56da13b`.
  - [x] **16d** ✅ (Day 30) — patient **My Tests** page (`my_tests` view: `appointment__patient=` filter + `select_related('appointment','result')`) showing test name/status/lab-tech note/result link. Design turn: built ReportLab branded-PDF download first, then **cut it** — tech's raw upload can carry real diagnostic content a text summary can't, and two downloads per test confused the UX; result link now points straight at `result.result_file.url`, `download_report` view/url/reportlab dependency removed entirely.
  - [x] **16e** ✅ (Day 31) — doctor-side test visibility. `test_detail` view (scoped `appointment__doctor=me` = IDOR guard) + template (name/status/normal/tech/date + `result_file.url` download). `doctor_today`/`doctor_records` loop `appt.lab_tests.all` — **loop OUTSIDE the status if/elif** (was gated behind CONFIRMED → vanished on COMPLETED rows where results matter); `{% empty %}` renders `-`. `prefetch_related('lab_tests__result')` (reverse FK one→many needs prefetch not select_related). `doctor_history`→`doctor_records` rename. Typos: `prefetech_related`, `test_details.html` mismatch, `appointment:` singular + missing `%` in `{% url %}`. **Closes Step 16.**
- [x] **Step 17 — prescriptions** ✅ (17a–17d, branch `feature/prescriptions`) — Prescription (OneToOne Appointment, medicines JSONField, only on COMPLETED), doctor write (dynamic rows = getlist pattern), patient view, PDF.
  - [x] **17a** ✅ (Day 32) — `prescriptions` app + `Prescription` model (OneToOne appt CASCADE = `appointment.prescription`; **first project `JSONField`** `medicines` default=list; diagnosis/advice/created_at) + admin + migration `0001`. COMPLETED-gate deferred to 17b view (not model). Zero typos.
  - [x] **17b** ✅ (Day 32) — doctor write page in `prescriptions` app: `PrescriptionForm` (diagnosis/advice; appointment+medicines NOT form fields) + `write_prescription` view (`get_object_or_404(..., doctor=me, status=COMPLETED)` = auth + COMPLETED-gate in one query; `instance=getattr(appt,'prescription',None)` = create-or-update, no OneToOne IntegrityError) + `getlist`×4 + `zip` + `if name:` → medicines JSON + cloneNode "+ Add medicine" + `prescriptions:write` route (config include) + COMPLETED-row button on doctor_records. Bug: `getElementByID` (JS case-sensitive → clone built but never appended, "+ Add" silently dead) → `getElementById`.
  - [x] **17c** ✅ (Day 33) — patient view page (`view_prescription`, `get_object_or_404(Prescription, appointment__id=id, appointment__patient=me)` = cross-FK IDOR lookup) + `prescriptions:view` route + read-only `view.html` (loops `medicines` JSON) + COMPLETED-row link on `my_appointments`. Doctor read-back = **nothing built** (17b write button already loads existing). UX: split doctor `Action` column → **Action / Tests / Prescription** on both `doctor_records` + `doctor_today` (latter gained a Prescription col). Killed reverse-OneToOne N+1: `select_related('...','prescription')` on both patient + doctor_today queries. Design: Request Test stays on CONFIRMED (live-consult order), not COMPLETED.
  - [x] **17d** ✅ (Day 34) — prescription "PDF" with **zero server code**. Chose `window.print()` + `@media print` CSS over ReportLab (the rendered HTML *is* the doc; no library/view/route/layout). `<style>` hides `header, footer, .no-print` on print; `<button onclick="window.print()">` → browser "Save as PDF". Default filename = `document.title`, so title block rebuilt to `Prescription -{patient}-{date|Y-m-d}` (browser only suggests). Also closed 17c gap: `view.html` never rendered `advice` → added `{% if prescription.advice %}` block. 3 silent template typos (Prince caught on test): `prescrition.advice` (missing `p` → blank under correct `{% if %}`), `prescription.appointment_date` in title (dropped `.appointment` hop). Template-only, no Python errors. **Closes Step 17.**
- [x] **Step 18 — notifications + background tasks** ✅ (18a–18e, PR #9, branch `feature/notifications`) — Notification model + 8 triggers (matrix) + bell + email + Celery/Redis. Sub-step split: 18a model · 18b in-app triggers · 18c bell UI · 18d **email + decouple/.env** · 18e Celery/Redis background tasks (email moved 18c→18d Day 37; Celery split 18d→18e Day 38 — email alone was a full session).
  - [x] **18a** ✅ (Day 35) — `notifications` app + `Notification` model (recipient FK→AUTH_USER_MODEL CASCADE `related_name='notifications'`, message, `notification_type` TextChoices 6 cats BOOKING/REMINDER/TEST/RESULT/PRESCRIPTION/STATUS, `link` plain CharField = stored URL string not FK, `is_read`, `created_at`, `ordering=['-created_at']`) + admin (`recipient__username` search) + migration `0001`. Data layer only — no triggers/bell/email. Zero typos.
  - [x] **18b** ✅ (Day 36, commits `4deb381` + docs `34b519e`) — `notifications/services.py` `notify(recipient, message, notification_type, link='')` = **single write path** (chosen over 7× inline `objects.create` and over signals — signals fire on every save incl. admin) + **7 triggers**: book→patient · reception_book→patient · confirm→patient · cancel→**other party** (`[patient, doctor]` + `if user != request.user` = 2 lines covers all 3 canceller roles; Model `__eq__` compares PK, safe vs `SimpleLazyObject`) · request_test→**all LAB users** (`User.objects.filter(role='LAB')` loop, one row each — `is_read` is per-row) · upload_result→patient **+** doctor (2 calls not a loop — different links) · write_prescription→patient. `reverse()` = Python `{% url %}`, `args=[...]` for params (**`check` does NOT catch `NoReverseMatch`** — resolved all 5 in shell). **Rule: `notify()` after `.save()`, INSIDE the state guard** (guard = idempotency; outside it a refreshed POST double-writes). Typo: `appointment.patent` → AttributeError *after* file saved + 1st notify written (half-done write; `transaction.atomic()` later). Accepted dupe: `write_prescription` is create-or-update → edit re-notifies (noise not bug, judge in 18c). Tested 4/7 incl. the branchy cancel; rest verify free once bell renders.
  - [x] **18c** ✅ (Day 37, 6 commits →`e657a5a`) — bell + in-app delivery. `notification_list` (`user.notifications.all()` = 18a related_name, ordering free) + `open_notification` (IDOR in lookup `recipient=request.user`; flips `is_read` **and** redirects — one user action, one view; `redirect(link or 'notifications:list')` since `link` blank-able). Routes `notifications:list`/`:open` + config include. **First context processor** `unread_count` — `{}` for AnonymousUser (**must never raise: runs every render**), else `.count()` (not `len()`); dotted path in TEMPLATES; alternative = 20 view contexts. Bell in base.html outside role chain, `{% if unread_count %}` hides `(0)`. List tmpl: unread `<strong>`, `get_notification_type_display`, `{% empty %}`. **Dropdown skipped** (rows every request for rarely-opened panel). All 7 triggers browser-verified. **Bell exposed 2 latent bugs** (fixed): cancel sent both parties to `my_appointments` (patient FK → doctor saw empty table) → per-recipient ternary; `'prescription:write'` singular in doctor_today (17c, only renders on COMPLETED). **`link` = frozen string** — view fix doesn't repair old rows (`.update(link=reverse(...))`). Prescription-edit dupe: **keep** (edit *is* news). Typos: `user.notification` singular, `<storng>`.
  - [x] **18d** ✅ (Day 38, 4 commits →`9438051`) — **email + first secrets out of git**. `pip install python-decouple` (was in stack list, never installed) → `backend/.env` (gitignored via `*.env`) + `.env.example` (committed; `*.env` matches files *ending* `.env`, so `.example` is safe). `SECRET_KEY`/`DEBUG` moved out of settings — `config('KEY')` no default = **required, boots or crashes**; **`cast=bool` mandatory** (`bool('False')` is `True` → prod would run DEBUG on). `EMAIL_*` block, `DEFAULT_FROM_EMAIL = EMAIL_HOST_USER` (Gmail rejects a From it didn't authenticate). Built on `console.EmailBackend`, flipped one string to `smtp.EmailBackend` to go live. `notify(..., email=False)` = **keyword arg with default → all 7 existing calls untouched**; `Notification.Type(x).label` = `get_..._display` from Python; `from_email=None` → DEFAULT_FROM_EMAIL; `and recipient.email` guard (LAB user is blank). 3 call sites flagged (book · reception_book · upload_result→patient). `fail_silently=True` = placeholder for 18e Celery retry (mail failure must not 500 a saved booking). **Matrix amended — doctor result email dropped** (see rule above). Live Gmail send verified shell + browser. Typos: missing `EMAIL_PORT` (default 25 → would timeout only after SMTP switch), `return Notification` (class not row — `check` passes, invisible till a caller uses the return).
  - [x] **18e** ✅ (Day 39, 7 commits `8c99fcc`→`ada4129`) — **Celery + Redis + Beat, closes Step 18.** Redis via Docker (`docker run -d -p 6379:6379 --name opd-redis redis`; daily = `docker start opd-redis`; `-p` mapping = the whole point). Wiring: `config/celery.py` (`os.environ.setdefault('DJANGO_SETTINGS_MODULE')` — worker is own process, never touches `manage.py`) + `config/__init__.py` import (**non-negotiable**: skip → `@shared_task` has no app, worker lists ZERO tasks, no error) + `namespace='CELERY'` + `CELERY_BROKER_URL` from `.env` + `CELERY_TIMEZONE = TIME_ZONE` (Beat's "8am" meaningless without). **Day-38 debt paid**: `fail_silently=True` → `autoretry_for=(Exception,)` + `retry_backoff` + `max_retries=3`, so `fail_silently=False` now *required* (exception = retry trigger; same line, opposite value, because the send moved to another process). `.delay(notification.id)` — **ID never object** (args are JSON; an object is a stale photograph). New failure mode: `.delay()` makes booking depend on Redis → guarded `except OperationalError` (narrow, not bare) + `logger.exception`. **2 scheduled tasks**: `send_appointment_reminders` (+ new `Appointment.reminder_sent` bool — **Beat WILL fire twice**; reusing Notification rows rejected, `link` is a string with no FK) · `expire_stale_appointments` (PENDING only, **never CONFIRMED**, tested with a CONFIRMED control row in range). Mechanics: `from .services import notify` **inside** task bodies = real circular import (services↔tasks); `timezone.localdate()` not `date.today()` (DB is UTC, 02:00 IST = yesterday UTC); `sent += 1` not `qs.count()` (lazy QuerySet re-runs → returns 0 after sending 50). `CELERY_BEAT_SCHEDULE` + `crontab` (expiry 00:30 before reminders 08:00). Windows: **`--pool=solo` mandatory** (no `os.fork()`), **worker never auto-reloads** — restart after every `tasks.py`/`settings.py` edit. Skipped `CELERY_RESULT_BACKEND` + `django-celery-beat`. 3 silent typos (below). ~2h20m vs 1:30 budget (Prince chose full 18e over split).
- [x] **Step 19 — dashboard + exports + history** ✅ (19a–19d, branch `feature/dashboard`, one PR). Excel **cut** (CSV opens in Excel; `.xlsx` = `openpyxl` for nothing).
  - [x] **19a** ✅ (Day 40) — reception dashboard numbers. `reception_dashboard` view in `appointments` app (**no new app** — zero models, it's a read over `Appointment`). `aggregate()` (→ one dict, not lazy) for the stat cards + **conditional aggregation** `Count('id', filter=Q(status=...))` = 7 counters in **one table scan** vs 5 `.count()` calls = 5 scans. `per_doctor` = `.values('doctor__username').annotate(total=Count('id')).order_by('-total')` = SQL `GROUP BY` + JOIN via the `__` span. `timezone.localdate()` not `date.today()` (18e trap: UTC DB, 02:00 IST = yesterday UTC). Role gate `raise Http404()` (file's existing pattern). Route `appointments:dashboard`, `dashboard.html` stat cards + per-doctor table, RECEPTION nav link. Inline `<style>` 4 rules (**no `static/css/` exists yet** — the frontend step builds it). Zero typos. Commits `9915e04`, `805e733`.
  - [x] **19b** ✅ (Day 40) — Chart.js bar (per doctor) + doughnut (per status). Zero new Python logic; only change = `list(...)` around `per_doctor` (**QuerySet is not JSON serializable** → `TypeError` in `json_script`; `aggregate()` already returns a plain dict, needs nothing). **`json_script`** = the Python→JS handoff: `{{ per_doctor|json_script:"id" }}` → `type="application/json"` (browser stores, never executes) + `<`/`>`/`&` escaped, so a username containing `</script>` can't break out. Naive `var d = {{ per_doctor }}` fails twice — Python `repr` single quotes = invalid JSON, **and** stored XSS since usernames are user input. Read with `JSON.parse(el.textContent)`. Chart.js via CDN `<script>` (no npm, no build step). Typo: **`getElementByID`** — 2nd time in project (17b killed "+ Add medicine"); worse here — `TypeError` on the first statement **aborts the whole `<script>`**, so *both* charts blanked from one word. Browser-verified against the table.
  - [x] **19c** ✅ (Day 41) — patient medical history. `medical_history(request, patient_id=None)` = **one view, two routes** (`history/` → own; `history/<int:patient_id>/` → staff). **2 IDOR guards, different jobs**: `else: raise Http404()` blocks the *caller* (patient guessing ids); `role='PATIENT'` inside `get_object_or_404` lookup blocks the *target* (doctor fishing another doctor's id) — a DOCTOR hitting a doctor id takes the `elif`, so `else` never runs, the lookup 404s. Query = `.select_related('doctor','prescription')` (JOIN, 0 extra — reverse OneToOne qualifies) + `.prefetch_related('lab_tests__result')` (2 queries, nested; many-side can't JOIN without duplicating the parent row) = **3 queries for 5 visits or 500**, vs `1+4×N`. Meta ordering gives newest-first free. Template is nested `<div>`s not a table (tree-shaped data); `{% if appt.prescription %}` safe in templates (`silent_variable_failure`); `appt.lab_tests.all` costs 0 because the *view* prefetched — **template cost is decided in the view**. Links: patient nav "My Health Record" + patient name wrapped in `<a>` on appointment_list/doctor_today/doctor_records (`appt.patient_id`, not `.patient.id` — FK column already on the row). Typos: `appointment/` singular template dir, missing `history/` route, unclosed `{% if advice %}` (loud, TemplateSyntaxError), `appt.lab_test` singular (**silent — whole block vanished**), `prescription:view` singular, `<storng>` (2nd time).
  - [x] **19d** ✅ (Day 41) — CSV export, **closes Step 19**. `export_appointments_csv` (RECEPTION-gated) — `HttpResponse(content_type='text/csv')` + `Content-Disposition: attachment; filename=` (**that header alone** is display-in-tab vs download). `csv.writer(response)` works because writer wants **anything with `.write()`**, not a file → no temp file, **zero new deps** (stdlib; why openpyxl/`.xlsx` was cut). **Extracted `_filtered_appointments(request)`** from `appointment_list` — shared not copied, so the export inherits its 3 filters *and* its `select_related('patient','doctor')` (else N+1: 500 rows = 1001 queries); returning a lazy QuerySet is safe, each caller finishes it. Button carries state: `?{{ request.GET.urlencode }}`. `get_status_display()` **needs `()`** in Python (template calls it for you). **CSV injection guard** `_csv_safe()` — leading `=`/`+`/`-`/`@` prefixed with `'` on the 2 user-typed columns (username, notes); Django's HTML escaping does nothing for a spreadsheet → **escaping is per-destination**. Typo: **`request.GST.urlencode`** → renders empty string → `?` with nothing after → export silently returned ALL rows while the view was correct (3rd silent-template-name bug of the session).
- [ ] **Step 20 — frontend + leftovers + README** (**swapped with deploy on Day 42, Prince's call** — deploying before the UI is rewritten means every CSS tweak costs a push + Render build, and Whitenoise/`collectstatic` gets wired twice; local runserver is instant). Stitch → Antigravity templates for all 4 roles, first real `static/css/`, **green/red availability calendar (deferred from Step 15 — DO NOT FORGET; native date input can't style dates, needs JS calendar)**, pro README (demo GIF, badges, Swagger shot). **Moved from Step 16 (Day 31): (a) forgot-password flow — Django `PasswordResetView` + reset templates, on top of Step 18 Gmail SMTP; (b) profile-photo upload — `ImageField` on CustomUser + Pillow + resize 400×400.** **Stack decided Day 42: Django templates + Tailwind CDN. React rejected** — DRF covers appointments only (Step 14), so a SPA needs ~20 new endpoints + JWT-in-browser replacing working session auth + a 2nd Render service, and rewrites `window.print()`/`json_script`/`getlist` for zero visible gain on a server-rendered CRUD app. **Figma rejected** — outputs a design, not code; Stitch emits pasteable HTML+Tailwind. Chain = Stitch → Antigravity (add `{% extends %}`/`{% url %}`/loops) → `frontend/templates/`. Prompt lives in **`frontend/stitch_exports/DESIGN_PROMPT.md`** (Part 1 = design system, pasted every time; Part 2 = one block per sub-step). Sub-steps (merged 8→6): **20a** ✅ (Day 42) shell — base.html + sidebar nav + **public landing page at `/`** (Prince's call Day 42: patients self-register, so the app needs a public face; replaces the `RedirectView`→login currently on `/`. Static page → `TemplateView`, zero view code, first class-based view in the project) · **20b** auth pages + forgot-password + profile photo · **20c** patient pages · **20d** doctor pages + write-prescription repeater · **20e** lab + reception (dashboard/charts/list/book-on-behalf) · **20f** availability calendar + notifications + README.
- [ ] **Step 21 — deploy (LAST)** — Whitenoise + prod settings (DEBUG off, `ALLOWED_HOSTS`, env vars, PostgreSQL) + Render + live URL. **Run `DEBUG=False` locally FIRST** — that alone catches most prod-only breakage (static files stop being served by the dev server, missing `STATIC_ROOT`, real 500 pages instead of tracebacks) without waiting on a build.

## Last 2 days

**Day 41 (2026-08-07) — Step 19c + 19d: medical history + CSV export, closes Step 19.** First page that asks for rows *and everything hanging off them*. Access decided before any code: **one view, two routes** (`history/` → own, `history/<int:patient_id>/` → staff), with **two guards doing different jobs** — `else: raise Http404()` blocks the *caller* (patient typing an id), `role='PATIENT'` inside `get_object_or_404` blocks the *target* (doctor fishing another doctor's id). For a DOCTOR on a doctor id the `elif` is True, so `else` never runs and the lookup 404s — which is why the role belongs *in* the lookup, not in an `if` after it. Query = `.select_related('doctor','prescription')` (JOIN, 0 extra; reverse OneToOne qualifies) + `.prefetch_related('lab_tests__result')` (2 queries, nested — many-side can't JOIN without repeating the parent row) = **3 queries for 5 visits or 500**, vs `1+4×N`. Template is nested `<div>`s not a table (tree-shaped data); `{% if appt.prescription %}` is safe in a template where Python would raise (`silent_variable_failure`); and `{% for test in appt.lab_tests.all %}` **looks like an N+1 and costs zero** — delete the prefetch and the template doesn't change one character while the page fires 4×N. **Template cost is decided in the view.** 19d: a response is bytes plus two labels (`content_type` + `Content-Disposition: attachment`), and `csv.writer(response)` works because the writer only ever wanted **something with `.write()`** — stdlib, **zero new deps**, which is the whole case against openpyxl. Filter logic **extracted not copied** into `_filtered_appointments()`, so the export inherits both the 3 filters and `select_related` (else 500 rows = 1001 queries); lazy QuerySet return is safe, each caller finishes it. **CSV injection**: leading `=`/`+`/`-`/`@` runs as an Excel formula on the receptionist's machine, so `_csv_safe()` prefixes `'` on the 2 user-typed columns — Django's HTML escaping bought nothing here, because **escaping is per-destination**. **7 typos, 6 silent** — `appointment/` singular template dir, missing `history/` route, `appt.lab_test` singular (**whole Lab Tests block vanished**, while the `{% for %}` two lines below spelled it right), `prescription:view` singular, `<storng>` (2nd time), and the best one **`request.GST.urlencode`** → empty string → `?` with nothing after → export silently returned every row **while the view was correct**. Only the unclosed `{% if advice %}` gave a line number. Three of them share one tell: the feature quietly does the *unfiltered* version of its job.

**Day 42 (2026-08-08) — Step 20a: order swap, Tailwind shell, landing page.** Two decisions before any code. **Deploy and frontend swapped** (Prince's call): deploying first means every CSS tweak costs a push + Render build, and Whitenoise/`collectstatic` gets wired twice — so frontend is Step 20, deploy is Step 21, with `DEBUG=False` **locally** first to catch most prod-only breakage without a build. **React rejected**: DRF covers appointments only, so a SPA needs ~20 endpoints + JWT replacing working session auth + a 2nd Render service, and rewrites `window.print()`/`json_script`/`getlist` for nothing a recruiter can see. **Figma rejected**: outputs a design, not code. Chain = Stitch → Antigravity (MCP) → templates, Tailwind by CDN, no npm. Rebuilt `frontend/stitch_exports/DESIGN_PROMPT.md` (gitignored) as **23 page-by-page sections**, each carrying its own facts + Stitch prompt + Antigravity prompt — and reading the real forms to write it corrected **6 wrong names** I'd have shipped: medicines are `med_name`/`med_dosage`/`med_frequency`/`med_duration` (not `medicine_name`…), register is `password`/`password_confirm` with only username/email/phone, `time_slot` is `<input type="time">` not a slot picker, breaks are `break_start`/`break_end`, `is_normal` is a checkbox not a radio pair, and the profile view is **read-only** (no form, no POST branch). Any one of those = a page that looks perfect and saves nothing. **`/` stopped redirecting to login**: `TemplateView.as_view(template_name='home.html')` — first class-based view in the project, zero view code, because the whole body would have been one line Django already wrote. Typo: `name='home'` placed **inside `as_view()`** → `TypeError: TemplateView() received an invalid keyword 'name'` at import; `as_view()` kwargs configure the *view*, `path()` kwargs configure the *route*. base.html came back from Antigravity with **`{% block content %}` twice**, once per auth branch → `TemplateSyntaxError` on every page: Django parses the file before it knows who's logged in, so "only one runs" doesn't help — a block name is an address. Fixed by branching only where the layouts differ (`<aside>` and `<header>`) and sharing one main/messages/footer; `ml-[240px]` had to stay conditional. Verified by **rendering**, not just `check` — anon page has no `<aside>`, all 4 roles get sidebar + POST logout, `/` returns 200 with exactly one header and one footer. Side finding: `ALLOWED_HOSTS` is already restricted (rejected `testserver`), one less Step-21 item.

## Day 43 resume point — Step 20b (auth pages), on `feature/frontend`

1. **New session** — `/clear`; point Claude at this resume + `PROJECT_LOG.md` Day 42 only.
2. **20a is DONE** — `base.html` (Tailwind CDN shell, role nav, one shared main/messages/footer) + `home.html` (public landing) + `/` served by `TemplateView`. Branch `feature/frontend`, PR at the end of Step 20.
3. **The build loop is in `frontend/stitch_exports/DESIGN_PROMPT.md`** (gitignored, local only). 23 page sections, each with its own facts + Stitch prompt + Antigravity prompt. Per page: `PART S` + page STITCH → Stitch; `PART A` + page ANTIGRAVITY → Antigravity (MCP); Prince types it in; compile-check; browser; one commit.
4. **Next up: PAGE 02 login → PAGE 03 register → PAGE 04 profile.** Login has **no form object** (view reads `request.POST['username'/'password']` directly) — keep plain `<input>`s, do not convert to `{{ form.x }}`. Register is `username`/`email`/`phone`/`password`/`password_confirm`, nothing else. Profile view is **read-only** (no form, no POST branch) — display only, editing + photo upload stay 20b/20f work.
5. **Verify by rendering, not by `check`.** `check` misses bad `{% url %}`, wrong template dirs, and duplicate block names. Use `get_template('x.html')` to compile, then `Client().get(...)` (set `settings.ALLOWED_HOSTS=['*']` in the shell — `testserver` is correctly rejected).
6. **Deferred, do not forget**: `static/css/` still does not exist (Tailwind CDN covers it so far); forgot-password flow (needs Step 18 SMTP); profile-photo upload; green/red availability calendar (PAGE 21 in the prompt file); the `@media print` block on the prescription page will need `height:auto; overflow:visible` overrides because base.html uses `h-screen overflow-hidden`.
7. **Carry-forward for Step 21 (deploy)**: Render env vars are all `config(...)` with **no default** — `SECRET_KEY`, `DEBUG`, `EMAIL_*`, `CELERY_BROKER_URL` — a deploy missing any one refuses to boot. Compare Render's env list against `backend/.env.example`. Decide **Celery/Redis in prod** (Render free tier has no Redis; `.delay()` call sites already guard `OperationalError`). `ALLOWED_HOSTS` is already restricted — verified Day 42.
8. **Session ritual** (only if testing notifications): `docker start opd-redis`, then 4 terminals — redis · runserver · worker `--pool=solo` · beat. Worker does **not** auto-reload.

## Running grep-list (silent typos — pass `check`, crash at request)

Scan user-typed code before declaring clean: enum-vs-field casing (`Model.Status.X` vs `.status`, both definition + usage) · namespace singular/plural (`appointment:` vs `appointments:`) · **template dir singular in `render()`** (`'appointment/x.html'` → `check` passes, `TemplateDoesNotExist` only when the URL is hit) · wrong namespace on shared name (`profile` = accounts) · transposed model attrs (`ForeingKey`, `roel`) · **return/branch indentation in multi-branch views** (mis-indented return → wrong role lands + dead code; valid Python) · context-dict key misspelling (`'appiontments'` → silently empty page) · JSON key typos (`'ends'`) · `forms.erros`/`.error` plural drops · `{% csrf token %}` missing underscore · `' %}`→`%'}` scramble · missing `%` in `{% url %}` · `<form action="post">` (should be `method=`) · loose HTML between `<td>`s (`check` passes, renders wrong) · quoted class in `router.register` · missing trailing slash on POST routes · **reverse-accessor singular/plural** (`user.notification.all()` vs `related_name='notifications'` → AttributeError at request) · **misspelled HTML tag** (`<storng>` → browser renders unknown tag as plain inline, styling silently vanishes) · **`return` merged onto the next statement's line** (`return send_mail(...)` where a bare `return` was meant → guard inverted, valid Python, task/view reports success doing nothing) · **misspelled model attr assigned beside a correct `update_fields`** (`appt.reminder_send = True` + `save(update_fields=['reminder_sent'])` → Python attaches a non-field attr, save writes the untouched real field, flag never persists) · **Celery task-name string** (`'apps.notification.tasks.x'` in `CELERY_BEAT_SCHEDULE` → Beat logs success, worker in *another process* logs `Received unregistered task`; diff against the worker's `[tasks]` banner) · **JS `getElementById` casing** (`getElementByID` → *not a function*, `TypeError` **aborts the whole `<script>` block**, so everything below it silently never runs — 2 occurrences: 17b, 19b) · **`values()` key in a template** (`row.doctor.username` where `values('doctor__username')` named it `row.doctor__username` → empty cell, no error; same for any dict-key typo like `stats.completd`) · **misspelled attr on `request` in a template** (`request.GST.urlencode` → empty string → querystring vanishes, export/link silently does the *unfiltered* version of its job; same silence as `appt.lab_test` singular and `stats.completd`) · **commented-out test setting left beside the live one** (two `'schedule'` keys, one `crontab(minute='*')` → ships to prod) · **kwarg placed inside `as_view()` that belonged to `path()`** (`TemplateView.as_view(template_name=..., name='home')` → `TypeError: received an invalid keyword` at import; `as_view()` kwargs configure the view, `path()` kwargs configure the route) · **duplicate `{% block X %}` in one template** (AI-converted base.html with one block per `{% if %}` branch → `TemplateSyntaxError: 'block' tag with name 'content' appears more than once`; Django parses the file before it knows which branch runs, so "only one renders" does not help) · **page-level `<header>`/`<footer>` kept in a child template that already extends base** (renders two of each; silent, looks like a CSS bug).

## Teaching-style rule (Day 9)

Simple English only — no fancy/status words (orthogonal, idempotent, ambient, etc).Real Django terms are fine (migration, ORM, decorator, serializer, QuerySet…) but each **new** one gets: **analogy** (everyday story, 2-4 lines — must WALK THROUGH the
connection step by step, each part of the story maps to a part of the concept, not just a 1-line label) → **plain meaning** (1 line) → **tiny code** (2-3 lines, from OUR app) → **why it matters** (1 line). Also say what physically happens in memory/DB/network, not just "prevents X". Skip the full pattern for concepts already taught.Voice anchors: `tutorial/03,05,07`. After teaching, ask 1-2 easy questions to check
understanding; correct wrong answers honestly, don't just say "close enough."

## Step chunking rule (Day 13)

Session budget = **1–1:30 hr**. BIG step (4+ files OR 5+ concepts OR 2+ hrs OR multi-feature) → split into sub-steps (13a…) each ~1–1:30 hr. Not big → one session. **One branch + one PR per parent step** (Option A). Enforced: (1) pre-session scope check + confirm before code; (2) hard stop at budget — WIP commit or revert, never "15 more min".

## Feedback rules (from memory)

- **Incremental tutorial** (Day 14): write tutorial + docs commit per sub-step, not bulk at parent end (exception: user may override, e.g. all-13e-together).
- **Architecture preference** (Day 22): user welcomes debate — make the technical case ONCE, then land on his call; don't re-litigate the same point across turns. He values clean industry structure (dedicated `api` app, org-by-layer).

## How to help

1. Explain before code; give the command/code to type.
2. One step at a time; wait for confirmation.
3. Refer to this file + `opd_roles_and_final_structure.html` — don't re-derive. Old step detail → `PROJECT_LOG.md`.
4. Update roadmap checkboxes when a step completes.
5. Typo-sweep user-typed code (grep-list above) before "no issues".
6. Caveman voice OK for summaries/headers when active; **clear prose for teaching**.

## Token efficiency (Day 28, revised Day 29, Day 30)

- **`PROJECT_LOG.md` = complete history, appended EVERY working day at wrap** (not only when a day rolls off CLAUDE.md). Never auto-loaded — open only to debug an old step. Entry format: `### Day N (date) — Step X shipped` + one paragraph (what shipped, key gotcha, commits).
- **CLAUDE.md** keeps static info + roadmap + **last 2 days** (short recap) + resume point + rules. Adding a new day → drop the oldest of the two (it's already in PROJECT_LOG, so nothing to migrate).
- **Wrap checklist**: tutorial section → tutorial/README → CLAUDE.md (roadmap + 2-day window + next resume) → **PROJECT_LOG.md append** → memory buffers → docs commit.
- One running grep-list (above), not per-day copies. Reference files, don't repaste. Review `/memory` weekly, delete superseded entries.
- **New session per parent step (Day 30)**: don't keep extending one long-running Claude Code session across unrelated sub-steps — each carried-forward turn re-sends the full conversation history (token tax scales with session length, not with the work left to do). Start a **new session** (`/clear` or new window) when beginning a new sub-step; point Claude only at this file's resume point (+ `PROJECT_LOG.md` if older detail is needed) instead of continuing an old thread.
